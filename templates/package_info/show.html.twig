{% extends 'base.html.twig' %}

{% block title %}PackageInfo{% endblock %}

{% block body %}
    <h1>PackageInfo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ package_info.id }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ package_info.price }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ package_info.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ package_info.description }}</td>
            </tr>
            <tr>
                <th>PackagePicture</th>
                <td>{{ package_info.packagePicture }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('package_info_index') }}">back to list</a>

    <a href="{{ path('package_info_edit', {'id': package_info.id}) }}">edit</a>

    {{ include('package_info/_delete_form.html.twig') }}
{% endblock %}
