{% extends 'base.html.twig' %}

{% block title %}Customer{% endblock %}

{% block body %}
    <h1>Customer</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ customer.id }}</td>
            </tr>
            <tr>
                <th>Gender</th>
                <td>{{ customer.gender ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>FirstName</th>
                <td>{{ customer.firstName }}</td>
            </tr>
            <tr>
                <th>LastName</th>
                <td>{{ customer.lastName }}</td>
            </tr>
            <tr>
                <th>Birthday</th>
                <td>{{ customer.birthday ? customer.birthday|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ customer.phone }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ customer.address }}</td>
            </tr>
            <tr>
                <th>CostumerPicture</th>
                {# <td>{{ customer.costumerPicture }}</td> #}
                <td><a href="{{ asset('uploads/' ~ customer.costumerPicture) }}">your picture</a><td>
                {# <img src="{{ asset('uploads/' ~ customer.costumerPicture) }}" /> #}

                
            </tr>
            <tr>
                <th>FidelityCard</th>
                <td>{{ customer.fidelityCard ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>
      
        <h3>RDV</h3> 
    {% for appointment in appointments %}
        
            <p>le {{appointment.date ? appointment.date|date('Y-m-d') : '' }} à
            {{ appointment.timeHour ? appointment.timeHour|date('H:i') : '' }} avec
            {{appointment.staff}}</p>   
        
    {% endfor %}

    {# {% for staff in staffs %}

    {% endfor %} #}
    

    <a href="{{ path('customer_index') }}">back to list</a>

    <a href="{{ path('customer_edit', {'id': customer.id}) }}">edit</a>

    {{ include('customer/_delete_form.html.twig') }}
{% endblock %}
